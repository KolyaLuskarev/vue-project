import { ref, computed } from 'vue'
import { defineStore } from 'pinia'

export const useNamesStore = defineStore('store', {
  state: () => ({
    /** @type {{ foodNames: string, prise: number, id: number }[]} */
    foodNames: [],
   
    nextId: 0,
    
  }),
  actions: {
 
  },
})